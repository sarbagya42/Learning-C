#include <stdio.h>
#include <stdlib.h>

int main()
{
    int num1, num2, sum;
    printf("Type two numbers:");
    scanf("%d" "%d",&num1, &num2);
    sum=num1+num2;
    printf("The sum is: %d",sum);
    return 0;
}
