#include <stdio.h>
#include <stdlib.h>
// Multiple String reading and printing

int main()
{
 char a[10], b[10],c[10];
 printf("Type 3 strings:");
 scanf("%s %s %s", a,b,c);
 printf("The strings are %s, %s, %s ", a,b,c);
    return 0;
}
